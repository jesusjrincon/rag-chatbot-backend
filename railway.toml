[build]
builder = "nixpacks"

[deploy]
startCommand = "python initialize_rag.py && gunicorn src.main:app --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build.nixpacks]
aptPackages = ["python3-dev", "gcc"]

